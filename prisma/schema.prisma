generator client {
  provider = "prisma-client-js"
}

datasource db {
  // Currently using Neon
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id String @id @default(uuid()) 
  username String
  password String
  email String
}